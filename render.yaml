services:
  - type: web
    name: wedding-invite-villa-romanov
    runtime: node
    plan: free
    buildCommand: echo "no build"
    startCommand: node server.js
    healthCheckPath: /
    envVars:
      - key: NODE_VERSION
        value: "22"
      # Задайте в панели Render вручную (пароль для /admin)
